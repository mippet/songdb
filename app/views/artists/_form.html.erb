<%= form_for @artist do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :name %>
  <%= f.number_field :age %>

  <%= f.label :image_url %>
  <%= f.text_field :image_url %>

  <%= f.submit (@artist.new_record? ? "Create" : "Update") %>

<% end %>
