<%= form_for [@artist = Artist.new] do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :image_url %>
  <%= f.text_field :image %>

  <div class="actions">
        <%= f.submit "Add Artist", class: "btn btn-success" %>
      </div>

<% end %>
